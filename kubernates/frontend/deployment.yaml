apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 5
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: login-gitops-demo-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
